-- select all 
SELECT * FROM minionsT;

-- select only names
SELECT name FROM minionsT;

-- select only id
SELECT id FROM minionsT;

SELECT * FROM towns ORDER BY name;

SELECT * FROM departments ORDER BY name; 

SELECT * FROM employees ORDER BY salary DESC; -- Избира всички от таблица 'employee', от най-висока заплата към кай-ниска